package tinker;

import java.util.HashMap;
import java.util.Map;

public class Command extends DataHolder{
	static Map<String, Command> commander = new HashMap<String, Command>();
	Command(){
		commander.put("login", new Login());
		commander.put("logout", new Logout());
		commander.put("info", new Info());
		commander.put("listavailable", new ListAvailable());
	}
	public void execute(String[] command){
	}
}
