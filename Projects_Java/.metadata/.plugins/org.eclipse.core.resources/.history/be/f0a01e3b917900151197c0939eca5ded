package testpack;

import java.util.Scanner;

public class CommandHandler {
	public static void main(String[] args){
		System.out.println("Enter command:");
		final Scanner in = new Scanner(System.in);
		final String command = in.next();
		final CommandHandler handle = parse(command);
		final String result = handle.run();
		System.out.println("Result: " + result);
	}
	
	private static CommandsHandler parse(String command){
		final String[] split = command.split(":");
		if (split[0].equals("sum")){
			return new CommandsHandler(split);
		}
		throw new IllegalArgumentException(
			"Unknown command " + command);
		}
		return null;
	}
}
